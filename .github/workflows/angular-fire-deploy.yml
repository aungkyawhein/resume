name: Deploy to Firebase Hosting using angular fire
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install -g @angular/cli
      - run: npm ci && npm run build
      - run: ng deploy
